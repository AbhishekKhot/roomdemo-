<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.Noteapp">

    <application
        android:name=".dependancy.MyNoteItemApplication"
        android:allowBackup="true"
        android:icon="@drawable/applicationicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoppingListApp">
        <activity
            android:name="com.example.Noteapp.ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.Noteapp.ui.NoteActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>